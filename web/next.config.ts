import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: true,
  // swcMinify: true,
  // experimental: {
  //   appDir: true, // Se estiver usando a pasta "app"
  // },
};

export default nextConfig;
